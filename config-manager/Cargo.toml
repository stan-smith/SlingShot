[package]
name = "config-manager"
version = "0.1.0"
edition = "2021"
description = "Configuration management for kaiju remote nodes"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "5"
base64 = "0.22"
thiserror = "2"
